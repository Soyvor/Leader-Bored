import React from 'react';

const Profile = () => {
    return (
        <div>
            <h1>Profile</h1>
            {/* Add content for Profile */}
        </div>
    );
};

export default Profile;